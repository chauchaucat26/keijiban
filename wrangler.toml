name = "my-app"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output"

[vars]
# Add non-secret env vars here if needed
# Secrets should be added in the Cloudflare Dashboard
